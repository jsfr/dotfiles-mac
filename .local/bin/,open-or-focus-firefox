#!/usr/bin/env sh

WINDOW_ID=$(yabai -m query --windows | jq -r '[.[] | select(.app == "Firefox" and .title != "Picture-in-Picture") | .id] | first')

if [ "$WINDOW_ID" != "null" ]; then
    yabai -m window --focus "$WINDOW_ID"
else
    open -a Firefox
fi
