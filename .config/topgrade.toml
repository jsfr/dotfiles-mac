# Same options as the command line flag
disable = ["emacs", "gem", "vim"]

# Custom commands
[commands]
"Neovim (Packer + Aniseed)" = "/Users/jens/.local/bin/sync-nvim"

# Additional git repositories to pull
[git]
max_concurrency = 5
repos = [
    "~/Repos/github.com/pleo-io/*"
]
